[project]
name = "anchor"
version = "0.1.0"
description = "Anchor — single-user private Telegram bot (Phase 1)"
requires-python = ">=3.12,<3.13"
dependencies = [
    "aiogram>=3.31.0,<4",
    "aiohttp>=3.14.3,<4",
    "sqlalchemy[asyncio]>=2.0.54,<3",
    "asyncpg>=0.31.0,<0.32",
    "alembic>=1.20.0,<2",
    "pydantic-settings>=2.15.0,<3",
    "openai>=3.16.2,<4",
    "trafilatura>=2.2.0,<3",
    "pyrage>=1.4.0",
    "boto3>=1.43.100",
    "pyyaml>=6.0.2,<7",
]

[dependency-groups]
dev = [
    "pip-audit>=2.10.1",
    "pytest>=9.1.1,<10",
    "pytest-asyncio>=1.4.0,<2",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]
