version: 2
updates:
  # uv reads pyproject.toml/uv.lock; Dependabot's "pip" ecosystem
  # understands both (pyproject.toml dependency blocks and a uv.lock
  # lockfile), so no separate "uv" ecosystem entry is needed.
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 10

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"

  # Phase 8: the vault service (vaultd/) has its own locks and image.
  - package-ecosystem: "pip"
    directory: "/vaultd"
    schedule:
      interval: "weekly"

  - package-ecosystem: "npm"
    directory: "/vaultd"
    schedule:
      interval: "weekly"

  - package-ecosystem: "docker"
    directory: "/vaultd"
    schedule:
      interval: "weekly"
